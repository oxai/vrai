
syntax = "proto3";

service DataComm {
   // (Method definitions not shown)
  rpc GetObs(Empty) returns (NeosObservation);
}

message Empty {

}

message NeosObservation {
    //int32 k = 1;
	float x = 1;
	float y = 2;
	float z = 3;
}
